const directLogin=document.getElementById("direct-login"),send=document.getElementById("send-contact"),requiredFields=["email","email","message"];directLogin.addEventListener("click",(function(){let e={};storeEmailOrTriggerError("email-login",e,"email"),sKeysInDict(e,["email"])||(alrtMessage.innerHTML="Please fill in all the required fields.",swapAlert("alert-warning","alert-success"))})),send.addEventListener("click",(function(){let e={};storeTextOrTriggerError("name",e,"name"),storeEmailOrTriggerError("email",e,"email"),storeTextOrTriggerError("message",e,"message",10,200),isKeysInDict(e,requiredFields)?sendRequest("POST",getConnection() + "/contact",e).then((function(e){console.log(e),"success"==e.message?(alrtMessage.innerHTML="Message sent successfully.",swapAlert("alert-success","alert-warning")):(alrtMessage.innerHTML="Failed to send message.",swapAlert("alert-warning","alert-success"))})).catch((()=>{alrtMessage.innerHTML="Server is not responding or you don't have any conection.",swapAlert("alert-warning","alert-success")})):(alrtMessage.innerHTML="Please fill in all the required fields.",swapAlert("alert-warning","alert-success"))}));

function getConnection() {
	current_url = window.location.href;
	url_object = new URL(current_url);
  
	protocol = url_object.protocol;
	domain = url_object.hostname;
	port = url_object.port;
  
	return `${protocol}//${domain}:${port}`;
}